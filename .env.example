# Copy this file to .env and adjust values as needed.
# These are the only environment variables the skeleton needs today.

APP_ENV=development   # development | production
PORT=8000             # port the API server listens on
